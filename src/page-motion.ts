export const PAGE_MOTION_POLICY = {
  scrollProgress: { normalMotionOnly: true },
  sectionReveal: {
    default: false,
    normalMotionOnly: true,
    durationMs: 220,
    translationPx: 12,
  },
} as const;
