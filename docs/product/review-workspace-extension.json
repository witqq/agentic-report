{
  "contractVersion": 1,
  "id": "review-workspace",
  "summary": "Adds a bounded offline thread protocol, deterministic source targets, and revision-segment binding for agent-to-human report handoff.",
  "trustBoundary": {
    "authorCode": "forbidden",
    "callbacks": "forbidden",
    "eval": "forbidden",
    "dynamicImports": "forbidden",
    "networkAccess": "forbidden",
    "confinement": "source-root",
    "offline": "required",
    "deterministicSerialization": "required",
    "cspCompatible": "required",
    "packageOwnedRuntime": "required"
  },
  "evidence": {
    "sourceGrammar": "Version-3 review JSON is a strict closed record with bounded subject threads and ordered revision segments; each segment owns its report revision, target, optional exact selected-text start/end target references with Unicode code-point offsets and NFC quote, ordered user/agent messages, and resolved state. Valid version-2 whole-block records normalize losslessly. Compile-time targets come only from parsed container directives and ordinary Markdown blocks, with authored ranges and optional explicit section identity.",
    "executionCapabilities": "The package-owned reader runtime maps valid native text ranges to deterministic anchors, opens one full-thread popover beside the selected or saved range, renders persistent open/resolved highlights with focusable overlay markers, and keeps Review as a non-reflowing list/import/export overlay. It stores session state in memory, validates exact local imports against reconstructed DOM ranges, and downloads every compatible legacy block thread and selected-text note in one canonical JSON file; it cannot rewrite Markdown, execute messages, register callbacks, sync a network account, or grant verified authority.",
    "confinement": "Review inspection first prepares the report source root, then resolves the relative review path through the existing URI-normalized lexical and canonical symlink confinement boundary before reading one ordinary bounded file.",
    "offlineBehavior": "Build, embedded target discovery, review parsing, and source binding use local source files and package code only; both output formats retain normal file URL behavior and the review command starts no listener or network request.",
    "deterministicSerialization": "Canonical version-3 serialization omits clocks and random values, sorts thread identities while preserving segment/message order, normalizes human text to Unicode NFC, distinguishes whole-block and exact range subjects, and is byte-equal for equivalent state; report revision separately binds the unchanged version-2 canonical target projection.",
    "cspAndRuntime": "The target manifest is escaped inert template text; package-owned controls use text/value DOM APIs, File text parsing, and a revoked local Blob object URL. No author script, innerHTML, storage, network, form submission, or new CSP source is added.",
    "accessibility": "The labelled Review entry is always available, while a valid selection reveals a native localized Create note button beside the range; Shift release focuses it for the keyboard route. Each saved highlight has a focusable overlay marker that exposes View thread. Popover, list, message, edit, resolve/reopen, close, import, and export controls use native keyboard behavior; labelled fields and alerts remain available, and modal mobile list focus is contained and restored.",
    "byteAndPerformanceBudgets": "Sidecar parsing and binding are bounded preparation work; browser initialization remains one manifest/prior-state pass. Highlight/popover geometry is coalesced through animation frames only while one of those surfaces exists, so an idle page schedules no continuous scroll-time work. Runtime and stylesheet byte budgets are enforced by unit and package checks.",
    "packageDependenciesAndLicenses": "No package or browser dependency is added; implementation uses existing Node crypto/filesystem APIs, unified traversal, React escaping, and the MIT-licensed package code already distributed by the project.",
    "compatibility": "Existing Markdown, whole-block thread data, build, validate, inspect, unchanged version-2 target manifests/report revisions, single-file and directory contracts remain supported. Valid version-2 review artifacts without selection data are accepted and remain list-only; the reader creates new threads only from exact selected text. Unsupported or disguised wider schema versions fail explicitly, and endpoint binding cannot impersonate changed content in a still-present source file."
  }
}
